{
  "name": "soa-exam-slidev",
  "private": true,
  "scripts": {
    "build": "slidev build",
    "dev": "slidev",
    "preview": "npm run build && npx vite preview",
    "export:light": "slidev export",
    "export:dark": "slidev export --dark"
  },
  "dependencies": {
    "@slidev/cli": "^0.40.16",
    "@slidev/theme-default": "*"
  }
}
